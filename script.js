  // 1
    // elseを使用するときは、７の値がtrueかfalseどちらなのかで処理をしなくてはならないのか考えながら作りました。

    // 2
    // 実装したときはデベロッパーを確認し、
    // エラーになってないか確認しながら作りました。
    
    // 3
    var variableNumber = 7 ;
    if(variableNumber){
      console.log("trueの処理");
      console.log(variableNumber);
    }else{
      console.log("falseの処理");
      console.log(variableNumber);
    }

    // 4
    var variableText = "text" ;
    if(variableText){
      console.log("trueの処理");
      console.log(variableText);
    }else{
      console.log("falseの処理");
      console.log(variableText);
    }